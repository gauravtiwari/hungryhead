<% data = {
    description: "#{@event.description}",
    event_url: "#{event_path(@event)}",
    is_user: @event.user.user == current_user
  }
%>
<%= react_component('EventDescription', {data: data.to_json} ) %>