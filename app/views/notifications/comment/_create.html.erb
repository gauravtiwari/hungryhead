<span class="inline">
  <% unless params[:controller] == "users" %>
    <div class="thumbnail-wrapper d32 fs-11 user-pic circular inline m-r-10">
      <% if activity.owner.avatar_present? %>
        <%= activity.owner.get_avatar %>
      <% else %>
      <span class="placeholder no-padding bold text-white">
        <%= activity.owner.name_badge %>
      </span>
      <% end %>
    </div>
    <strong class="inline text-black"><%= activity.owner.name %></strong>
  <% end %>
  posted a comment on <%= activity.recipient_type == 'Idea' ? activity.recipient.name : 'a' + activity.recipient_type %>
</span>