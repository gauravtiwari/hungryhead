var $el = $('#quickview-conversations');
$("#conversation-messages-view").html("<%= j render partial: 'conversation_messages_view' %>");

$("#conversations-list-view").hide();
$("#conversation-messages-view").show().addClass('animated fadeIn');

$.Pages.init();

$('body textarea').on('focus', function(){
  $(this).autosize();
});