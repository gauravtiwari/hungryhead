var $new_content = $("<%= j render partial: 'activities' %>");
window.activityListView.append($new_content);
ReactRailsUJS.mountComponents();

$('body textarea').on('focus', function(){
  $(this).autosize();
});

$('a.load-more-posts').attr('href', "<%= root_path(page: @activities.next_page) %>");

<% unless @activities.next_page %>
  $('.pagination-box').remove();
<% end %>