var $el = $('.activity-feed-box');
$el.append("<%= j render partial: 'organizations/content/activities' %>");
ReactRailsUJS.mountComponents();
$('body textarea').on('focus', function(){
	$(this).autosize();
});	
$('a.load-more-posts').attr('href', "<%= organization_path(page: @activities.next_page) %>");
<% unless @activities.next_page %>
	$('.pagination-box').remove();
<% end %>