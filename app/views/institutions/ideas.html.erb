<div id="content">
	<!-- profile header -->
	<div class="organization-box">
		<% if user_signed_in? && current_user  &&  @organization.organization_cover !=  nil %>
		<%= react_component('UniversityCover', render(template: 'organizations/show.json.jbuilder', :prerender => true)) %>
	<% end %>
		<div class="container">
			<%= render partial: 'organizations/partials/organization_data' %>
			<%= render partial: 'organizations/partials/follow_list' %>
		</div>
	</div>
	<!-- end profile header -->
	<!-- profile menu -->
	<%= render partial: 'organizations/partials/menu' %>
	<!-- end profile menu -->
	<!-- Content 
	================================================== -->
	<div id="content" class="container">
		<div class="row">
			<div class="col-md-4">
				<%= render partial: 'organizations/_sidebar/about_organization' %>
				<%= render partial: 'organizations/_sidebar/top_entrepreneurs' %>
			</div>
			<div class="col-md-8">
				<div class="infinite-scroll animated fadeIn">
		  			<div class="ideas project-columns infinite-scroll-container">
						<%=  render partial: 'organizations/content/ideas' %>
					</div>
				</div>
			</div>
		</div>
	</div>
	<!-- End content -->
</div>