
var $el = $('body');
$el.append("<%= j render partial: 'popup' %>");
$('#newSharePopupModal').modal('show');

$('body').on('focus', 'textarea', function(event) {
  event.preventDefault();
  $(this).autosize();
});