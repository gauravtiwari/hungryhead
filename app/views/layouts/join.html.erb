<!DOCTYPE html>
<html>
  <head>
    <%= <%= metamagic title: "Hungryhead", description: "Hungrhead is a platform for students to share, collaborate and validated startup ideas with peers. ", keywords: %w(entrepreneurship startup idea students schools colleges universities) %> %>

    <%= favicon_link_tag 'favicon.ico' %>

    <meta content="hungryhead team" name="author" />

    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-touch-fullscreen" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="default">

    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>

    <!--[if lte IE 9]>
      <%= stylesheet_link_tag 'core/css/ie9',  media: 'all', 'data-turbolinks-track' => true %>
    <![endif]-->

    <script type="text/javascript" data-turbolinks-eval=false>
      window.onload = function() {
        // fix for windows 8
        if (navigator.appVersion.indexOf("Windows NT 6.2") != -1)
          document.head.innerHTML += '<%= stylesheet_link_tag 'core/css/windows.chrome.fix' %>'
      }
    </script>

    <%= csrf_meta_tags %>
    <%= yield :head %>

  </head>

  <body class="fixed-header menu-behind <%= user_signed_in? ? '' : 'no-header' %> <%= controller_name %> <%= action_name %>">
    <% if browser.mobile? %>
      <%= render partial: 'globals/app_sidebar' %>
    <% end %>
    <div class="page-container">
      <% if user_signed_in? && !browser.mobile? %>
        <%= render partial: 'globals/nav', locals: {bg: 'bg-white'}  %>
      <% else %>
        <% if user_signed_in? && browser.mobile? %>
          <%= render partial: 'globals/page_nav_mobile' %>
        <% else %>
          <%= render partial: 'globals/join_nav' %>
        <% end %>
      <% end %>

      <% if user_signed_in? %>
        <div class="page-content-wrapper">
          <div class="content">
            <%= yield %>
          </div>
        </div>
      <% else %>
        <%= yield %>
      <% end %>
    </div>
  </body>

</html>
