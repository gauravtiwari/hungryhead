<% cache(['beta', 'why', 'v1', "#{browser.mobile? ? 'mobile' : 'desktop'}"]) do %>
  <div class="learn-wrapper bg-white">
    <div class="learn">
      <div class="jumbotron no-margin" data-pages="parallax" data-social="cover">
        <div class="why-photo">
          <div class="container vmiddle">
          <div class="row <%= browser.mobile? ? 'p-b-40 p-t-20' : ''  %>">
                <div class="col-md-8 col-sm-10 auto-margin">
                    <div class="hero-section inner text-center">
                        <h1 class="text-white">Learn the reason</h1>
                        <p class="text-white fs-16">
                          Why we are doing, what we are doing?
                        </p>
                    </div>
                </div>
            </div>
        </div>
        </div>
      </div>
    </div>

    <div class="what-section bg-white">
      <div class="<%= browser.mobile? ? 'wrapper-small' : 'wrapper-lg'  %>">
        <div class="col-md-6 col-sm-10 auto-margin">
          <%= render partial: 'pages/content/why' %>
        </div>
      </div>
    </div>

    <section class="bg-master-lightest full-border-light text-center wrapper-lg full-border-light-bottom no-overflow" id="intro">
      <div class="col-sm-8 col-md-8 auto-margin">
        <h1 class="m-t-5 m-b-20">
          Hungryhead is a new way to start entrepreneurship.
        </h1>
        <p>Things you may want to know before getting started</p>
        <%= link_to('Click here', learn_path, class: 'main-button m-t-10 fs-13 bold') %>
      </div>
    </section>

  </div>
<% end %>
<%= render partial: 'globals/footer' %>
