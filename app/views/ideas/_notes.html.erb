<%= javascript_tag do %>
	var idea_collaboration_channel = pusher.subscribe("presence-idea-collaboration-<%= @idea.team.join('') %>");
<% end %>

  <%= react_component('OpenNotesBox') %>
  <div id="quickview" class="notes-quickview-wrapper" data-pages="quickview-notes">
  <ul class="nav nav-tabs">
    <li class="active">
      <a href="#quickview-notes" data-toggle="tab">Add Notes</a>
    </li>
  </ul>
  <a class="btn-link quickview-toggle" data-toggle="quickview-notes"><i class="pg-close"></i></a>
  <div class="tab-content">
        <div class="tab-pane fade in no-padding active" id="quickview-notes">
<%= react_component('Notes', {form: {user_id: current_user.id, action: idea_notes_path(@idea), csrf_param: request_forgery_protection_token, csrf_token: form_authenticity_token}}.to_json) %>
</div>
</div>
</div>

