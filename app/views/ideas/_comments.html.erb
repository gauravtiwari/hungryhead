<% commentable = @idea %>

<% data = render(template: 'comments/comments.json.jbuilder', locals: {comments: @comments, record: commentable, paginate: false} )%>

<%= react_component('CommentBox', {data: data, standalone: true, comment_channel: "#{@idea.class}-#{@idea.id}-comments", count: @idea.comments_counter.value, comments_path: comments_path(commentable_type: @idea.class.to_s, id: @idea.id)}) %>