<div id="content">
	<!-- profile header -->
	<div class="organization-box">
		<% if user_signed_in? && current_user  &&  @organization.organization_cover !=  nil %>
		<%= react_component('UniversityCover', render(template: 'schools/show.json.jbuilder', :prerender => true)) %>
	<% end %>
		<div class="container">
			<%= render partial: 'schools/partials/organization_data' %>
			<%= render partial: 'schools/partials/follow_list' %>
		</div>
	</div>
	<!-- end profile header -->
	<!-- profile menu -->
	<%= render partial: 'schools/partials/menu' %>
	<!-- end profile menu -->
	<!-- Content 
	================================================== -->
	<div id="content" class="container">
		<div class="row">
			<div class="col-md-4">
				<%= render partial: 'schools/_sidebar/about_organization' %>
				<%= render partial: 'schools/_sidebar/top_entrepreneurs' %>
			</div>
			<div class="col-md-8">
				<div class="infinite-scroll animated fadeIn">
		  			<div class="ideas project-columns infinite-scroll-container">
						<%=  render partial: 'schools/content/ideas' %>
					</div>
				</div>
			</div>
		</div>
	</div>
	<!-- End content -->
</div>