var $el = $('.activity-feed-box');
$el.append("<%= j render partial: 'institutions/content/activities' %>");
ReactRailsUJS.mountComponents();
$('body textarea').on('focus', function(){
	$(this).autosize();
});	
$('a.load-more-posts').attr('href', "<%= institution_path(page: @activities.next_page) %>");
<% unless @activities.next_page %>
	$('.pagination-box').remove();
<% end %>