---
production:
  postgresql:
    configuration:
      postgis: true
      version: "9.4.3"
  rails:
    configuration:
      extra_packages:
        - chrony
      nginx:
        cors:
          methods: "GET, OPTIONS"
          origin: "*"
